# ManageIQ Pluggable UI


## How to enable a plugin
- Add plugin to /config.js
- Set 'consumes' in /plugins/core/package.json to what you are providing ("provides") in /plugins/[plugin_name]/package.json